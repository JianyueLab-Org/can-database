---
/**
 * 把文案里的 `**强调**` 渲染成 `<strong>`。
 *
 * 语言文件里有两句写着 markdown 的粗体，而渲染它们的地方都是纯文本插值，所以屏
 * 幕上一直留着字面的星号：`fixes.description` 的「**一个代号只在一个 FIR 内唯
 * 一**」和 `airports.magneticNote` 的「**磁**航向」。
 *
 * **不是删掉星号了事。** 那两处的强调都是有分量的：磁/真航向搞错会把一架停好的
 * 飞机歪在自己的机位框里（`[icao].astro` 里那段注释说的就是这件事），而代号只在
 * 单个 FIR 内唯一正是航路点页必须先选 FIR 的原因。把加粗抹平等于把这一页最该被
 * 看见的那半句降成和别的字一样。
 *
 * 按 `**` 切开、奇数段包 `<strong>`，**而不是拼一段 HTML 交给 `set:html`**：这样
 * 每一段仍然由 Astro 自己转义，不需要在这里再养一个 `escapeHtml`（地图那边已经有
 * 一个，理由见 `mapBase.ts`）。文案是我们自己写的常量，但一个不需要信任假设的写
 * 法比一个需要的更好。
 */
interface Props {
  text: string;
}

const { text } = Astro.props;
const parts = text.split("**");
---

{
  parts.map((part, i) =>
    i % 2 === 1 ? <strong class="font-semibold text-ink">{part}</strong> : part,
  )
}
