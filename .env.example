# can-database —— 本地开发。
#
# 四个都不是密钥，是地址。**这个站一行数据库凭据都没有**，也永远不该有：
# PostgreSQL 的口令只在 can-db（另一个仓库的 Go 服务）那里。

# can-db 的 API。本地就是你自己起的那一个。
#
# 线上这个值是**集群内**的 Service 地址，不是 api-db.ceruleanavi.net —— 那条公网
# 主机名是给集群外的消费者（can-atc）用的，见 src/lib/config.ts。
CAN_DB_ORIGIN=http://127.0.0.1:8080

# 会话在这里解，资料库权限也从这里读。
CAN_API_ORIGIN=https://api.ceruleanavi.net

# 没登录的人送去哪儿。
CAN_WEB_ORIGIN=https://ceruleanavi.net

# 本站自己的对外地址，写操作的 Origin 比对基准。本地一定要设。
PUBLIC_ORIGIN=http://localhost:4329
