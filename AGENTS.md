# AGENTS.md

给在这个仓库里工作的人和模型看的。`CLAUDE.md` 是指向本文件的软链接。

## 这是什么

**航行资料控制台** —— `database.ceruleanavi.net`，Cerulean Aviation Network 的第八
个 Astro 站点，也是 **can-db 唯一的前端**。

它显示机场、跑道、机位、航路点、航路和进离场程序 —— SweatBox 生成器、航路解析和管
制工作站都要读的那批资料。

技术形状和 can-portal / can-controller / can-efb / can-dev / can-radar 一样，**不要
在这里发明第九套**：Astro SSR（standalone Node 适配器）+ Vue 岛屿 + Tailwind v4 +
can-ui，跑在 jyl-tyo 上，由 CI 部署，Ingress 走 `cloudflare-tunnel`。

## 它是从 can-db 里拆出来的

can-db 从前是一个仓库两半：Go 服务加一个 `web/` 目录。**拆开的规矩是一句话 ——
can-db 用 Go 管后端，前端全在这里，而所有逻辑归 can-db。**

「所有逻辑归 can-db」不是口号，拆的时候真的搬了两处：

- 总览页从前取全部 dataset，自己 filter 出 `active`、把机场数加起来、把来源去重。
  **「什么叫在服务的数据」是一条关于这批数据的规则**，不是渲染细节。现在是
  `GET /api/v1/aip/overview` 一条路由答完，can-db 那边配了测试（`internal/aip` 的
  `TestOverviewCountsOnlyActiveDatasets` 钉的就是「loading 的那一期一个数都不该
  进」）。
- 航路点页从前为了画一个 FIR 下拉框，把 233 条机场索引取回来去重。现在是
  `GET /api/v1/aip/firs`。

**在这里写任何一行「算」出来的东西之前，先问它是不是一条关于数据的规则。** 是的话
它属于 can-db —— 这个仓库存在的意义就是它不回答那种问题。排序、搜索框的即时过滤、
把 procedure 分成 SID 和 STAR 两栏来显示，这些是渲染，留在这里。

## 三条不变量

**没有数据库口令。** 一条都没有，将来也不该有。全网能连上那个 PostgreSQL 的只有
can-db 一个进程。

**没有 Secret，一个都没有。** 和 can-efb、can-controller 一样。会话是 can-api 签在
**父域** `.ceruleanavi.net` 上的那一枚 cookie，成员在主站登录过，浏览器本来就把它带
到这里来；这个站做的全部事情是把它**原样转发**给 can-db，由那边去问 can-api「这是
谁、有没有 aipAccess」。多存一份 `SESSION_SECRET` 等于多一处能签发任何人身份的地方。

**整站要登录，而且整站要授权。** 一页公开的都没有，将来也不该有 —— 这个站显示的是
有许可限制的航行资料，而「有哪些机场」本身就已经超出一个匿名访客该看到的范围。

门槛是 `canUseConsole(aipAccess)`，也就是 **2、4 或 5**（ADM 在 can-api 的 `user` 表上
授予那一列）。**不是 `>= 1`，也不看教员评级** —— 那两条是「调用」那一档的门槛，而这
个站是「阅读」。

| 级别 | 名称       | 数据            | 用法                 |
| ---- | ---------- | --------------- | -------------------- |
| 0    | 无权访问   | ——              | ——                   |
| 1    | 可调用     | 公开            | 只走接口，不进控制台 |
| 2    | 可阅读     | 公开            | 接口 + 控制台        |
| 3    | 受限可调用 | 公开 + 官方汇编 | 只走接口，不进控制台 |
| 4    | 受限可阅读 | 公开 + 官方汇编 | 接口 + 控制台        |
| 5    | 管理/编辑  | 全部            | 全部，包括所有写操作 |

许可轴累积：3 及以上看得到官方汇编。1–4 的用途轴看奇偶：**单数只能调用**（别的服务替
成员从接口取数 —— can-portal 的 SweatBox 生成器、EFB、雷达都是这么用的），**双数才进
得了控制台**。用途轴不是有序的：3 比 2 大，但 3 进不来。**5 级在奇偶规则之外**：进得了
控制台，也是唯一的写权限档。常量和 `canUseConsole` 在 `src/lib/config.ts`；这条规则
没有上游副本 —— can-db 只把 cookie 转给 can-api 的 `GET /api/v1/auth/session`，自己判
`CanRead()` / `CanWrite()`。

**教员评级不开这道门。** can-db 的 `CanRead` 放教员过，是为了让生成器替他们取数 ——
那是接口，不是这个站。

**这一次「便利不是边界」要反过来读。** 别的站上中间件挡人只是为了不让人看见点下去
必然 403 的链接。这里多了一件事：**页面是服务端渲染的，机场清单在 HTML 里就是明
文**。所以那道门不只是「别让他看见入口」，它同时是「别把资料渲染给他」。把
`src/middleware.ts` 里那个数字改小，泄漏的是数据本身。

## 接缝

### 岛屿 → can-db：走本站的同源反代

`src/pages/api/v1/[...path].ts`，**白名单是重点不是修饰**。一个通配的 `/api/*` 转发
等于把 can-db 整个 API 挂到公网上。每一条都写着谁在用它 —— 要加页面就要加条目，而
且要顺手写清楚「谁在用」，否则以后没人敢删任何一条。

服务端渲染的页面不走这条，走 `src/server/canDb.ts`（它转发 cookie）。

**漏一条的症状是一片空白，不是一个错误。** 反代答 404，而 `api()` 刻意不抛异常，于是岛
屿把失败当成「这一层没有数据」—— 屏幕上是一张没有扇区的地图、一个没有地面线画的机场，
看起来像库里就没有。扇区图层是上线之后才发现漏了 `aip/sectors/*` 的。

所以有 `src/lib/allowList.test.ts`：它扫 `src/components/` 和 `src/lib/` 里每一处
`"/api/v1/…"` 字面量，逐条问 handler 用的那个 `allowed()`。**两张表的解析收在
`allowed()` 里，测试和 handler 用同一个** —— 测试里重写一遍解析顺序，第三张表加进来那天
它会安静地继续绿。

它挡不住两件事，写在测试的抬头里：路径整段来自变量时扫不到（今天没有这种写法），以及
can-db 那边有没有这条路由它不知道（放行一条不存在的路径，拿到的是 can-db 的 404）。

**改接口仍然是两个仓库的事**，这条测试只自动化了这一半。

### can-db 的地址是**集群内**的，即使它有公网主机名

can-db 从 2026-08-18 起有了 `api-db.ceruleanavi.net`，但这个站仍然打
`app.can-db.svc.cluster.local`。那条公网地址是给**集群外**的消费者用的 —— can-atc 是
控制员机器上的桌面程序，解析不了 ClusterIP。这个站和 can-db 在同一个集群里，走
Service 少一跳，而且不会因为 Cloudflare 隧道抖一下就连自己的后端都读不到。

### `checkOrigin` 必须是关的

`astro.config.mjs` 里 `security.checkOrigin: false`。Astro 从 `Host` 头推导本站
origin 再和浏览器的 `Origin` 比对，而这个站跑在 TLS 终止的反代后面，推出来的是
`http://…`、浏览器发的是 `https://…`，**永远对不上，每一个 POST 都是 403**。
can-dev、can-radar、can-efb 都是踩了才关的。

关掉不等于不检查：写操作的 Origin 比对显式配置的 `PUBLIC_ORIGIN`，那个值反代动不了。

## 这个仓库为什么必须是公开的

**JianyueLab-Org 是 GitHub Free 计划，组织级 secret 到不了私有仓库。** 部署要的
`KUBECONFIG_B64` 和 `GHCR_PULL_TOKEN` 都是组织级的，所以仓库只要是私有的，CI 就拿不
到它们 —— 表现为 deploy 在「Check cluster credentials」那一步失败。can-controller 最
初建成私有正是这么红了一次，can-efb 至今卡在这条上。

**它能公开是因为它什么都不装：** 没有凭据、没有 Secret、没有一行有许可的航行资料，
也没有 can-db 那个描述私有 `Sector` 仓库布局的提取脚本。控制台渲染的每一个字节都是
can-db 现给的。

改回私有 = CI 立刻不能部署。

## 页面的外框：内边距是 AppShell 给的，页面只定宽

can-ui 的 `AppShell` 已经把正文区连内边距一起给了：

```
<main class="flex-1 py-8 lg:py-10">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><slot /></div>
</main>
```

所以**页面自己那一层只写 `mx-auto w-full max-w-*`，一个 `px-` 或 `py-` 都不要加**。

这条是补上的，因为原来八个页面每个都又套了一层
`mx-auto w-full max-w-5xl px-4 py-8 sm:px-6 lg:px-8` —— 和 AppShell 的那一层逐字相
同，于是**左右内边距和上下留白都是加倍的**：桌面上下 2.5 + 2 = 4.5rem，左右 2 + 2 =
4rem，而**手机上左右各 1rem 加 1rem，375px 的屏幕正文只剩 311px**。这个站装的是宽表
和地图，那 32px 是白扔的。它不是审美问题，是量得出来的。

**宽度两档，按页面装什么分：**

| 档          | 给谁                   | 现在是谁                                                 |
| ----------- | ---------------------- | -------------------------------------------------------- |
| `max-w-6xl` | 清单、宽表、整幅的图   | `/airports`、`/positions`、`/fixes`、`/datasets`、`/map` |
| `max-w-5xl` | 读一条记录、填一个表单 | `/`、`/airports/<icao>`、`/route`                        |

原来这两档是混着用的，而且看不出规律 —— 两个清单页一个 5xl 一个 6xl，`/positions`
那张最宽的表反而是窄的那一档。**不要再引入第三档**：7xl 是 AppShell 的上限（写了等于
没写），更窄的话这些表会先撑不开。

`/denied` 和 `/404` 不在这张表里：它们自己居中，`denied` 走的还是 `shell={false}`。

## 三个排版组件，页面不要再拼类串

`src/components/` 下三个都是 `.astro`，都不带 `client:` 指令 —— 纯 HTML，零运行时。

- **`PageHeader.astro`** —— 每一页都从它开始。字号用 can-ui 的 `.text-title-1`
  （`clamp()`，字距行高跟着字号走），**不要再手写 `text-2xl … sm:text-3xl`**：那是在
  两个断点上各钉一个值，中间那段视口宽度得不到照顾。说明保留 `max-w-2xl`（这也是没
  有直接用 can-ui 那个 `PageHeader.vue` 的原因 —— 它不设上限，而这个站正文列宽到
  6xl，`fixes` 那句 65 字的说明会铺满整行）。`title` 插槽给机场详情页的等宽 ICAO，
  `actions` 插槽给右对齐的分类标签。
- **`SectionTitle.astro`** —— 小节标题 + 条数 + 可选说明。字号是 `.text-title-3`，
  **比原来的 `text-sm` 全大写重一档**：原来它和表头同号同色，于是小节标题在屏幕上并
  不比它下面那张表的表头更重，一页读下来分不出分界。条数 `0` 也渲染 —— 这一页是校对
  数据的，「没有机位」和「没渲染出来」不能长得一样。
- **`RichText.astro`** —— 把文案里的 `**强调**` 渲染成 `<strong>`。按 `**` 切开、奇
  数段包标签，**不拼 HTML 交给 `set:html`**，所以每段仍由 Astro 自己转义，不需要在这
  里再养一个 `escapeHtml`。

**语言文件里带占位符的键，别当纯文本用。** `airports.stands` 是 `"{n} 机位"`，机场详
情页曾经写成 `t("stands")`，于是屏幕上是字面的 `{n} 机位 (520)`。小节标题要的是不带占
位符的那一个（`airports.standsTitle`）—— 两个键并排放在语言文件里，分工在那里就看得
见。

## 航路和点的画法照 can-radar

同一个网络的两张图不该有两套读法：成员在雷达上认得的三角形航路点、压在腿中间的航路名，
点进资料库应该还是那一套。出处是 can-radar 的 `RadarMap.vue`，搬过来的部分在
`src/lib/mapBase.ts` 下半段。

四条约定，每一条都是那边踩出来的：

- **名字建在 marker 里，用容器上的一个 class 开关，不用 tooltip。** tooltip 要悬停、一次
  只出一个，而看图的人问的是「这一片点都叫什么」。切一个 class 也比在几十个 marker 上重
  建 DOM 便宜。
- **三个缩放阈值，不是一个**：航路名 5 级（少而关键，一屏放得下整条航路时 `W47` 正是要读
  的东西）、航路点 6 级、终端点 9 级 —— 一条 SID 把六个点铺在一个进近区里，早出就是把程
  序写在自己身上。
- **航路名标在腿上，每一段都标。** 名字属于那条腿；一条长航路只在中间标一次，等于放大去
  看某个点的人正好看不到它是哪条航路。**程序不标**：十几条腿写十几遍同一个词。
- **程序虚线、航路实线**，转折的那条腿同时属于两段，线在换样式的地方才没有缺口。

线走大圆插值（`arc()`）：直接连两个经纬度画出来的是墨卡托直线，跨度一大就和真航路差得出
来。

**航路页也是个例外，而且是我照搬时没换场景踩出来的。** can-radar 每条腿都标航路名，那是
因为雷达上一屏只有航路的一小截 —— 名字得出现在你正在看的那一段上。而航路页是**整条航路在
一屏里**：ZGGG→ZBAA 38 条腿，照搬的结果是 `A461` 沿着线重复三十几次糊成一片，航路点又因
为 6 级的阈值而在 `fitBounds` 出来的 5 级上一个名字都没有 —— 一屏灰线加几十个无名三角形。

改成**按航路串来标**：名字出现在换航路的地方，中间那些点只决定线的形状。

- 航路名**一段一个**，标在这一段的中点；很长的一段每 8 条腿补一个，免得放大后视野里没有
  名字。ZGGG→ZBAA 由 50 个标签降到 7 个。
- 换航路处的那个点（也就是航路串里出现的点）名字**一直显示** —— 那一行字就是这一页的产
  物，藏到某个缩放之后等于把答案藏起来。ZGGG→ZBAA 是 MIKIP、BUBDA、DUGEB 三个。
- 中间的点只画三角形，名字 9 级才出。

**全网图是个例外，而且是有意的。** 那里有 3914 个航路点和 11988 段航路，全建成 divIcon 会
掉帧（点之所以是 circleMarker 就是这个原因）。所以同一套读法换个落地方式：只给**视野内**
的画名字，**够放大**才画，而且**有上限**（`syncLabels`）。三个条件缺一不可 —— 少了视野是
全网几千个标签，少了缩放是缩到看不清时糊成一片，少了上限是有人放大到航路枢纽上时突然几百
个。航路名在那里一条只标一次（标在可见的最长一段的中点），不像航路页那样每段都标：一屏可
能有几十条航路。

## 三张图

`/map` 是全网图，机场详情页顶上是机场图，`/route` 上是规划出来的那一条。三张都共用
`src/lib/mapBase.ts`（瓦片、主题跟随、FIR 配色），**共用是必须的**：一个成员在清单上认
得的 ZGZU 的颜色，点进地图应该还是那一个，两处各写一份配色漂移只是时间问题。机场清单上
那排色块也取自同一个 `firColor`。

几条踩过或差点踩到的：

**`leaflet.css` 在 `AppLayout.astro` 里引，而且排在 `globals.css` 前面。** 从岛屿里引
的话它是一张动态样式表，浏览器插到 `<head>` 最后 —— 排在整站样式**后面**，于是 Leaflet
自带的 `.leaflet-control-attribution a { color:#0078A8 }` 赢过主题规则，版权条上留一串
默认亮蓝色链接。can-radar 踩过。

**地图挂在哪个容器上要**判断，不能只判断「有没有地图」。 航路页整段结果是
`v-if="plan"`，而 `submit()` 开头会清空 `plan` —— 每生成一次，地图那个 `div` 都会被拆掉再
建一个新的。旧地图还活着，只是挂在一个不在文档里的节点上，于是**第一次好、第二次空白**。
`draw()` 里比对 `map.getContainer() !== host.value`，不一样就把旧的 `remove()` 掉重建，顺
手停掉上一次的主题订阅 —— 不停的话每重建一次多一个，它们抓着旧闭包不放。

**地图岛屿必须 `client:only="vue"`。** Leaflet 在模块顶层就要 `window`，SSR 会直接炸。

**而且 Leaflet 只许出现在 `src/lib/mapMarkers.ts` 和地图岛屿里。** `mapBase.ts` 是「常量和
纯函数」—— 它被 `Airports.vue`、`Fixes.vue` 这些**服务端渲染**的岛屿 import（只为了
`firColor` 和瓦片地址）。往 mapBase 顶上加一行 `import L from "leaflet"`，机场清单页的每
一个请求都会炸在 `window is not defined` 上，而且**本地 build 一声不吭** —— 那是运行时的
错，不是构建期的。犯过一次，所以 `bun run build` 末尾跑
`scripts/check-ssr-browser-only.mjs`：服务端产物里出现浏览器专用包的静态 import 就红。

**Leaflet 对象放 `shallowRef`，不放 `ref`。** 它们是庞大的自引用结构，深响应式代理既
没用又很贵。

**三千段航路是一条 `L.polyline`，不是三千条。** 后者是三千个 SVG 元素，平移一次浏览器
就重排三千次。多段线接受「线的数组」，画出来一样。

**航路点只在选中 FIR 后才画。** 全网 15278 个点糊成一片，读不出任何东西；单个 FIR 最多
3914（RJJJ）还在能读的范围。所以不选 FIR 时那个开关是禁用的，而不是按了没反应。

**无坐标的程序点是断口，不是 0,0。** 那 311 个点会把线拉到几内亚湾。`AirportMap` 遇到
它就把线**切成两段**，而不是连过去。

**可疑的程序段画出来并标红，不藏。** 见 can-db 的〈程序点的解析在日本会挑错点〉：56 条
SID 的首点落在几百到一千多公里外。这个站是拿来校对数据的，把可疑的一段藏起来，看图的人
会以为数据是干净的。判据（离本场 > 400 km）和那份记录一致 —— 改一处要改两处。

**popup 里的文本一律 `escapeHtml`。** 机场名和航路点代号来自扇区包，是贡献进来的数据，
不是我们写的常量。

**航路图的线从起飞机场开始，而航段里没有那个坐标。** 一段航路只带它**到达**的那个点的
坐标，所以照着 `legs` 画出来的线，落地机场在图上、起飞机场不在 —— 看起来像少了一头数
据。can-db 为此在 plan 上直接给了 `fromLat/fromLon`、`toLat/toLon`（`RoutePlan`），
`RoutePlanner.vue` 的 `draw()` 用它起头。

## 全网图的扇区图层：三种状态，不是两种

`/map` 上第四个图层画的是**我们实际划的**那 569 块扇区（can-db 的 `network_sector`，来自
扇区包的 `[AIRSPACE]`），不是汇编发布的 594 个。

**几何一条都不在这里算。** 共享边接成闭环、容差 11cm 是量出来的下界 —— 那些是 can-db 导
入时定死的。这一页只画。**圆不离散化**：569 块里有 140 块是圆，`shape === "circle"` 时
`vertices` 是空的，圆心加半径才是它的几何（`L.circle`，半径海里 × 1852）。把圆当成「顶点
还没导进来」，屏幕上就会缺一块空域而**没有任何报错**。

**「没解析过」「有人管」「没人管」是三种状态。** can-db 显式返回 `uncovered`，理由是「没人
管」和「没查到」在地图上长得一样；这里再加一件 —— 还没输入在线名单。把没解析过的画成「没
人管」，等于在用户什么都没做时告诉他全网一块都没人管。三种画法：包色淡填充 / 归属色半透明
填充 / **红色虚线且不填充**。判据在 `src/lib/sectorMap.ts` 的 `sectorState`，弹窗和上色走
**同一个**函数 —— 两处各判一遍就会出现「画成没人管、弹窗里什么都不说」。

**大的先画，小的后画。** 扇区是竖着摞起来的（同一片地面上常有塔台、进近、区域好几块），
而 Leaflet 的点击只命中最上面那一个。按包围盒从大到小加进去，点下去命中的是最小最具体的
那块，大块在它自己独占的地方仍然点得到。这挡掉的是最常见的那种错：想点塔台却弹出区调。
要「一个点上摞着的全部列出来」得自己做点在多边形内的判断 —— 没做。

**按包取，不在浏览器里筛。** 包名就是 FIR 代号，所以 FIR 筛选条直接就是它的筛子；缓存按
包分开，来回切不会重下。**选不到的包画不出来**：`/aip/firs` 给的是有机场的 FIR，而
`PRC_FSS` 这类包不在其中 —— 它们只在「全部 FIR」下出现。

**top-down 是按钮，不是 `watch`。** 每敲一个字符打一次接口，中间态（`ZBAA_CT`）是一个不
存在的呼号，图会在打字过程中闪成一片「没人管」—— 而「没人管」是这张图上最需要可信的状态。

**规则不在这里。** 「沿链找第一个在线的」写在 can-db 的 `ResolveTopDown`；控制台要列、排班
要用、在线图要画，三处各实现一遍迟早不一致。这一页只把它的答案上色。

**悬空的一环画成「解析不到」，不跳过。** owner 链上 `callsign` 为 null 表示那个标识在它那
个包里找不到席位（全库 46 处，6 个标识）。跳过它会让 rank 出现空洞，而 rank 就是 top-down
的全部意义。

`sectorMap.ts` 单独成文件是因为这几条**错了屏幕不会说话**：颜色错了仍然是一张好看的图，
画序错了仍然每块都在。和 `taxiwayLabels.ts` 同一个理由 —— 判据能单独测就单独测。

## 程序那一段是岛屿，因为它是清单页

ZSPD 有 **122 条程序、1479 条腿**。原来是三栏静态卡片、每条程序把腿拼成一行等宽字 —— 一
屏放不下三条。这一段因此适用〈清单页的两条规矩〉：搜索框是主操作，截断要说出来。

三个筛子对应三种真实问法，不是三种排序：**按跑道**（放行前挑程序）、**按类别**、**按名字
或点**（核对某个点在不在这条程序上）。腿默认收起 —— 全展开是一万多个节点，能看见的不到
二十条。

**高度限制不解码。** `alt` 是 ARINC 424 的编码（`05910B03940A`），表下那句说明必须留着：
它长得像一串数字，读的人会以为是米或英尺。

## 表格用滚动阴影，不用 can-ui 的 `scroll-edge-*`

`.scroll-edge-y` 是一层 mask：上下各淡出 1.25rem。它是给「内容滚进浮层底下」写的，用在表
格上有两个毛病，两个都见过：

1. **不管有没有溢出都淡。** 一张三行的表，首行末行照样被糊掉一截 —— 看起来像渲染坏了。
2. 套在**横向**滚动的容器上，淡的是**另一个轴**：表格上下虚化，而真正该提示「右边还有」
   的左右两侧什么也没有。`datasets.astro` 就是这么写的。

换成 `globals.css` 里的 `.scroll-shadow-x` / `.scroll-shadow-y`：四层背景，两层盖片跟着内
容滚（`background-attachment: local`），两层阴影钉在边缘（`scroll`）。滚到头时盖片压住阴
影 —— **不能滚就一道阴影都不出现**，这正是 mask 做不到的。

**盖片颜色必须和容器背景一致**，默认 `--surface-raised`（这些表大多在 `.card` 里），不在卡
片里的用 `style="--scroll-shadow-bg: var(--surface)"` 覆盖。暗色主题下这两个差一档，蒙错
比不蒙还显眼。

## 跑道是一张表，不是两张

几何（入口/终点坐标、磁航向）来自导航数据，物理（长宽、道面、强度、入口标高）来自汇编，
两份数据各自按 (机场, 代号) 解析 —— 一条跑道可能只有其中一半。**按代号拼成一行**，缺的那
半是破折号。并排两张表要读的人自己对代号，而他问的是同一件事：这条跑道是什么样。

## 席位页只有一份席位了

`/positions` 列的是**本网实际开的** 888 个席位 —— 扇区包 `[POSITIONS]` 里的呼号、频率和
二次雷达码段。成员要开席位，问的是「我能开什么、频率多少」，而那只有这一份答得了。

**这一页从前有两份。** 汇编 `CONTROLLED` 切出来的 594 个管制扇区曾经并排摆在下半页作参
照（官方怎么划扇区）。can-db 把 `/api/v1/aip/positions` 换成扇区包那份之后它没有数据源
了，所以这里连 `Positions.vue`、`Unit`/`Position`/`PositionFrequency` 三个类型和十四个
i18n 键一起删掉 —— **留着一个填不满的组件，下一个人会以为它坏了，而不是以为它没了**。

汇编那三列（`unit` / `unit_kind` / `sector_code`）**还在 can-db 的库里**，只是没人读。要
它回来是那边的一次决定，不是这里补一个 interface。

**打的是 `/positions/network` 而不是 `/positions`**，虽然两条答同一份。两个仓库各自部署：
can-db 那边上线之前 `/positions` 还答着汇编那份（形状是 `Unit[]`），这一页拿到它一行都渲
染不出来。等 can-db 上线之后换过去，那条别名就可以删。

**呼号不唯一，所以按呼号分组。** `RJTG_CTR` 有 19 条，标识 TG/T01…T27 各管一个扇区，
EuroScope 靠标识把扇区归属解析回呼号。一行一个呼号，只有多于一条时才能展开 —— 单条的展开
按钮是禁用的，而不是点了没反应。所以页头的两个数**分开说**：**888 个席位、824 个不同呼号**
（线上查过的数），它们回答的不是同一个问题 —— 「多少人能同时上线」问的是后者。

**按归属包筛，不按 `alsoIn`。** 一个席位只由归属包定义为准，`alsoIn` 里的是邻包的副本、
对账用的。把它也算进筛选，选 ZSHA 会筛出一批实际归 RKRR 的席位。表格里那个 `+N` 角标就
是 `alsoIn` 的条数 —— 888 行里只有 104 行有。

服务端渲染：这份数据一个扇区包版本内不变，没有理由让浏览器再取一次。

## 清单页的两条规矩

**能被搜的清单一定要有搜索框。** 航路点页原来只有一个 FIR 下拉和一个滚动框，而 RJJJ 有
3914 个点 —— 打开那一页的人几乎总是在找某一个代号，唯一的办法却是用眼睛扫，或者按浏览
器的 Ctrl+F 去搜一份已经渲染出来的 DOM。搜索框是这一页的主操作，不是附加功能。

**截断必须说出来。** 航路点页只渲染前 300 行（三千多行表格是一万多个节点，而能看见的不
到三十个），机场详情页的机位表默认只渲染前 60 行，另有「显示全部」按钮。两处都把省略掉
的数量写在下面 —— **一个悄悄截断的列表会让人以为剩下的不存在**，那比慢糟得多，而这个站
的用途正是校对数据。

**清单页用同一套零件。** 搜索框是 `src/components/ui/SearchField.vue`（`/` 聚焦，Esc
清空，Enter 在只剩一条时直接打开），筛子是 `FilterChips.vue`，排布是
`src/components/lists/ListToolbar.vue`。筛选值经 `src/composables/useQueryState.ts` 写进
查询串（`replaceState`，初值在 `onMounted` 里读，免得水合对不上）：

| 页面               | 查询参数                                       |
| ------------------ | ---------------------------------------------- |
| `/airports`        | `q` `fir` `sort`                               |
| `/fixes`           | `fir` `q`                                      |
| `/positions`       | `q` `fir`（归属包） `facility`                 |
| `/airports/<icao>` | `stand` `proc` `kind` `rwy`                    |
| `/map`             | `fir` `layers`                                 |
| `/route`           | `from` `to` `level` `unrestricted`（提交时写） |

一页只有一个搜索框占 `/`：机场详情页给程序，机位表的 `hotkey` 是 `null`。

`DatasetExport.vue` 和 `AirportExportScope.vue` 不 import 任何 `.vue` 文件：
`src/lib/export/page.test.ts` 只编译这两个 SFC，多 import 一个组件测试就挂。

## 航路生成器：这一页不规划航路

`/route` 收两个机场代号和一个可选高度层，打 `GET /api/v1/aip/route`，把回来的结果画出
来。航路串里带 SID 和 STAR，但**没有两端机场** —— 机场是 from/to 两个字段。走哪条程序由
can-db 那边的搜索挑（每条程序是一条候选边，不是按代号排序取第一条），这里只显示。

**这一页最重要的一个字是 `source`。** `published` 表示这是汇编自己发布的城市对航线（全库
10367 个机场对有），`computed` 表示库里没有，这条是按航路网算的最短路。两者不是同一种答
案 —— 一条是「该怎么飞」，另一条是「按距离该怎么飞」，而后者不知道军航活动区和协议路线之
类图上没有的东西。所以来源标签紧跟在航路串下面，加一句话说明，**不要挪进折叠区**。

顺带一个数字上的坑：`distanceKm` 是机场到机场，`publishedDistanceKm` 是汇编印的数、量的是
航路段。**别拿后者去和大圆比** —— ZGGG→ZBAA 的航路段是 1625 km 而大圆 1880 km，比出来是一
条「比直线还短」的航路。绕行百分比只能用 `distanceKm`。**规划在 can-db**（`internal/aip/route.go`），理由和总览页那两个统计一样：第二个消
费者（管制工作站要航路线、can-efb 要飞行计划）不该把它再实现一遍，而且没有任何测试盯着
两份实现是否一致。

这一页因此只做三件事，每一件都是渲染：

- **把限制原文摆出来，不摘要。** 「W13航线限飞行高度8400米(含)以上使用」这种句子，摘一
  次就可能把含义摘反。can-db 也不解析它 —— 它只负责挑出沿途命中的那几条。
- **三种命中要看得出区别。** 每条限制带一个 `scope`：`route` 是汇编挂在**这条发布航线**上
  的那一句（最强，排最前），`segment` 是本次真的飞到了发布的那一段，`airway` 是只有航路
  对上（汇编里「W13航线…」说的是整条航路，却只挂在其中几段上，所以两种都得列）。不同颜色
  的标签加一句解释，**不要合并** —— 合并等于告诉看的人它们一样确定。
- **把 `notes` 单独用一张卡片显示。** 一条从最近航路点接入的航路，和一条走发布 SID 的航
  路，在图上长得一模一样，而这个区别对拿去放行的人很重要。藏起来等于说这两者一样。
- **提交前就说代号不在库里。** 提交后拿一个 404 也能懂，但那时人已经在怀疑是不是服务坏
  了。

- **空域卡片说的是「会穿过」，不是「不能飞」。** 禁区 can-db 在规划时就绕开了，所以这里
  只会出现限制区和危险区；它们的活动时间是「byNOTAM」「每日0700-0830」这类原文，谁也没解
  析。那句说明必须留着 —— 一个红色的空域标签很容易被读成「这条航路不能用」。
- **每个列表字段都兜一次底。** 这个岛屿是 `client:only`，模板里一处
  `plan.restrictions.length` 读到 `null` 就是一个 TypeError，Vue 停止渲染，**页面上什么都
  不剩** —— 不是报错，是空白。can-db 有过一次：发布航线且零条限制时 Go 的 nil 切片序列化成
  了 `null`，症状是「有些航路一生成 UI 就没了」，而算出来的航路好好的，所以看着像随机。那
  边修了也钉了测试，这里照样 `?? []`：后端的一次回归不该让整页消失。
- **最低超障高度低于请求的巡航高度时标红。** 那是 can-db 算的（`levelBelowMtca`），这里
  只跟着显示，不要在这边再判一次。

**要加高度层筛选、绕飞、备降之类的东西，那是 can-db 的路由参数，不是这里的一段
JavaScript。**

## 地面线画：按需取，画原色，把精度写在旁边

机场图上多了一个「地面线画」图层，数据是 can-db 从航图上抠出来配准好的
（`/api/v1/aip/airports/{icao}/ground`）。三条规矩：

**按需取。** 一个大场是五千多条线、两万多个点，跟机场详情一起拖等于每次打开机场页都多
下一兆的几何 —— 而多数人来这一页是看跑道和频率的。所以它是单独一条路由，勾上才发请求，
取过一次就留着。

**画图上的原色，不重新配色。** 那些线**没有语义** —— can-db 只存得到颜色和线宽，没有一
个字说哪条是滑行道中线、哪条是机坪边界。自己配色等于替它做一个没有依据的分类。原色画出
来就是原图的样子。线宽只用来分粗细（压到 0.5–2 像素），不按米当像素画，否则缩到全场时
整张图会糊成一块。

**精度要写在旁边。** 接口给的 `accuracyM` 是**该信的那个数**（5 米或 20 米），不是残差；
`runways = 0` 时还要多说一句「没有跑道可核对」—— 那种图整体可能偏二十米上下。校对数据的
站把误差藏起来，看图的人会以为它准。

## 写界面：只给 5 级

5 级「管理/编辑」看得到三样东西，别的级别一样都看不到：

- **`/datasets` 的「管理」列**（`DatasetActions.vue`）：复制为新一期、生效、停用、修改门槛，
  以及去编辑页和修订记录的链接。复制的周期号预填 `GET /aip/airac` 的 `next`，可以改。
- **`/datasets/[id]/edit`**（`DataEditor.vue`）：从登记表选表，按键列（子表加父列）筛选、
  分页列出，逐行新增 / 修改 / 删除。表单按登记表的列类型生成；删除前有页内确认框。子表按
  登记表的 `parent` 导航；登记表标了 `onAirport` 的表从 `airport` 的一行跳过去，按 `icao` 筛好。子表的
  父列在修改时锁定。
- **`/datasets/[id]/revisions`**：修订记录，服务端渲染，按 can-db 的 `next` 游标翻页
  （`?before=`），每行可展开列级改动。

`/datasets` 顶上的当前周期和下一周期（带生效日）对所有进得来的人显示。

**门槛是 `canManage(aipAccess)`，即 `aipAccess === ACCESS_MANAGE`**（`src/lib/config.ts`）。
两个页面在 frontmatter 里判，不满足时渲染 404 页；`datasets.astro` 按它决定渲不渲染管理列。
侧栏不变：两个新页面都挂在某一期下面，没有全局入口。

**这一层不判写权限。** 写路由在 can-db 走 `withWrite`（5 级），克隆、行编辑、约束校验和修
订记录都在那边。这里只把输入框的字符串变成 JSON 值（数字、布尔、数组），错误原样显示：违反约
束时 can-db 回 400/409 带 `constraint` 和 `column`，`api()` 把两个字段带出来，表单标在对应的
列旁边。

**反代白名单里的写条目**（`src/pages/api/v1/[...path].ts` 的 `ALLOW_PATTERNS`）：

| 路径                                             | 方法                  | 谁用               |
| ------------------------------------------------ | --------------------- | ------------------ |
| `aip/datasets/{id}`                              | PATCH                 | 修改门槛           |
| `aip/datasets/{id}/(activate\|supersede\|clone)` | POST                  | 生效 / 停用 / 复制 |
| `aip/datasets/{id}/tables/{table}/rows`          | GET POST PATCH DELETE | 数据编辑器         |

`{id}` 是 1–10 位数字，`{table}` 是 `[a-z_]{1,40}`。修订记录、登记表（`aip/tables`）和
AIRAC 日历（`aip/airac`）由 frontmatter 经 `callDb` 取，**不在白名单上**。写方法照旧先过
Origin 检查，再把 `origin` 头带给 can-db。

`allowList.test.ts` 把模板字面量里的 `${…}` 按表达式名换成样例值：含 `table` 的换 `airport`，
以 `id`/`Id` 结尾的换 `1`，其余换 `ZBAA`。给数据集 id 和表名的变量起名时照这个规则。

## 本地开发

```bash
bun install
bun run dev            # :4329
bun run lint           # format:check + astro check + vue-tsc —— CI 就跑这一条
bun run build
```

端口 4329 接着那条阶梯：4321 can-web、4322 can-dev、4323 can-radar、4324 can-efb、
4325 can-exam、4326 can-controller、4327 can-ui、4328 can-portal。

本地跑要给 `PUBLIC_ORIGIN`，否则写操作的 Origin 检查会拿线上地址去比对本地的
`http://localhost:4329`：

```bash
PUBLIC_ORIGIN=http://localhost:4329 CAN_DB_ORIGIN=http://127.0.0.1:8080 bun run dev
```

`CAN_DB_ORIGIN` 兜底是 `http://127.0.0.1:8080` 而不是某个 https 地址：本地开发时你
自己起一个 can-db。写一个公网地址当兜底会让「忘了配」悄悄变成「打到了别的东西上」。

## 上游

can-db 在 **JianyueLab**（不是这个组织）：`https://github.com/JianyueLab/can-db`。
它是私有的，理由写在它自己的 AGENTS.md 里 —— 简单说不是因为它藏了凭据（没有），而是
`scripts/build-sweatbox.mjs` 描述了私有 `Sector` 仓库的文件布局。

改接口是**两个仓库的事**：can-db 加路由、加测试，这里加白名单条目和类型。两边 CI 谁
也看不见对方。
